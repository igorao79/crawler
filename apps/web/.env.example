# Server API URL (Railway backend)
NEXT_PUBLIC_API_URL=https://your-server.up.railway.app

# Proxy URL — same as API_URL in production (proxy is integrated)
NEXT_PUBLIC_PROXY_URL=https://your-server.up.railway.app
